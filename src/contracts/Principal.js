pragma solidity ^0.5.0;

// Change the contract name as you wish
// Don't forget to rename the file .sol

contract Principal {
	string public name;

	constructor() public {
		name = "Principal";
	}
}
